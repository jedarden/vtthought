# FastAPI and WebSocket
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
websockets>=12.0

# ASGI server for production
gunicorn>=21.2.0

# Pydantic for data validation
pydantic>=2.5.0
pydantic-settings>=2.1.0

# Python 3.12+ compatibility
python-multipart>=0.0.6

# CORS support
python-dotenv>=1.0.0

# HTTP client for extension testing
httpx>=0.26.0

# Logging
python-json-logger>=2.0.7

# JWT Authentication (ADR-002)
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
python-multipart>=0.0.6

# OAuth (ADR-002) - Google OAuth support
authlib>=1.3.0
httpx>=0.26.0

# Database (ADR-011)
aiosqlite>=0.19.0
cachetools>=5.3.0
types-cachetools>=5.3.0

# STT Engine (ADR-005)
faster-whisper>=1.0.0
# deepgram-sdk>=3.0.0    # Fallback, optional

# Audio processing (ADR-004, ADR-005)
numpy>=1.24.0

# LLM Post-Processing (ADR-006)
ollama>=0.1.0

# Development dependencies
pytest>=7.4.0
pytest-asyncio>=0.23.0
black>=23.12.0
ruff>=0.1.0
mypy>=1.8.0
